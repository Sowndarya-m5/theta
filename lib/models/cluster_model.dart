class ClusterDataModel {
  final String name, reglink, ImageUrl, desc, Boxcolor, HeadName, Headclass, Headcontact;
  
  ClusterDataModel(
    this.name,
    this.ImageUrl,
    this.desc,
    this.Boxcolor, this.reglink, this.HeadName, this.Headclass, this.Headcontact,

    );
}
